spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring
    username: postgres
    password: c25358585
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true

